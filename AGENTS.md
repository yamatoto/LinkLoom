# LinkLoom プロジェクト向けエージェントガイド

## 目的
- この文書は LinkLoom 開発チームがエージェント（AI アシスタントや自動化スクリプト）を安全かつ効率的に運用するための指針をまとめています。

## エージェントの分類
- **開発支援エージェント**: コードレビュー、自動テスト、リファクタリング提案を担当します。
- **運用監視エージェント**: ログ監視、アラート通知、簡易な障害対応を実施します。
- **サポートエージェント**: ユーザーからの問い合わせに対する一次応答や FAQ の更新を支援します。

## 運用ルール
- 各エージェントには GitHub Personal Access Token を直接渡さず、リポジトリごとの限定トークンを用意します。
- 重要な設定値は Supabase Vault または Vercel 環境変数で一元管理し、平文での共有を禁止します。
- Slack 通知は `#alerts-linkloom` チャンネルに集約し、担当者のエスカレーション手順を明確にします。

## 品質管理
- 自動コード修正は Pull Request ベースで行い、最低 1 名のレビュー承認を必須とします。
- Playwright と Vitest の回帰テストを毎回実行し、失敗時は手動確認を行います。
- エージェントのモデルやプロンプトを更新する際は `docs/agents/CHANGELOG.md` に記録します。

## セキュリティ
- 外部 API との通信はすべて HTTPS を使用し、IP 制限が可能な場合は許可リスト方式を採用します。
- 定期的に依存パッケージを `npm audit` で精査し、Critical 以上の脆弱性は 24 時間以内に対応します。
- エージェントが保存する一時ファイルは 48 時間以内に自動削除されるよう設定します。

## 監査と改善
- 月次でエージェントの成果と課題をレビューし、KPI に対する達成度を共有します。
- 重大インシデント発生時には Root Cause Analysis を 72 時間以内に完了し、再発防止策を策定します。
- 新規エージェント導入前に PoC を実施し、パフォーマンス・コスト・リスクを評価します。

---
本ガイドはチームの運用状況に合わせて継続的に更新してください。
