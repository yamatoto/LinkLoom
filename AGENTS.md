# LinkLoom エージェント運用ガイド

## 目的

- LinkLoom を個人開発で進める際に、エージェント（AI アシスタントや自動スクリプト）が安全かつ継続的に開発を支援できるよう、共通のワークフローと注意事項を整理する。
- 全エージェントは作業開始前に `docs/plan.md` を確認し、進捗状況と優先タスクを把握すること。

## エージェントの役割

- **開発支援エージェント**: 実装、テスト、リファクタリング、ドキュメント整備など開発に関するタスクを担当。
- **ドキュメント/タスク管理エージェント**: `docs/plan.md` を基にタスクのステータス管理、実行結果の整理、必要に応じた更新案の提示を行う（更新はユーザー許可後）。

## 運用ルール

- 作業着手時は `docs/plan.md` の「次にやるべきこと」「進捗」を確認し、そこに記載された内容に沿ってタスクを選択する。疑問点はユーザーへ確認してから進める。
- タスク完了後は結果をユーザーへ報告し、`docs/plan.md` の更新が必要か確認を取ってから反映する。無断編集は禁止。
- リポジトリ操作はローカルブランチで行い、Pull Request ベースの運用は必須ではないが、複数の変更をまとめる場合は PR を推奨。
- 機密値や API キーは `.env` などローカル環境変数で管理し、平文での共有を避ける。必要に応じて Supabase Vault や Vercel 環境変数を利用する。
- ユーザーからコミット作成を依頼された際は、一般的なベストプラクティス（動詞始まりの簡潔なサマリ行 + 必要なら詳細説明）に沿ったコミットメッセージをエージェントが自動で用意すること。

## 品質管理

- コード変更を行う際は、影響範囲に応じて `npm run test`（Vitest）や Playwright の E2E テストを実行し、結果を報告する。実行が難しい場合は理由と想定リスクを明示する。
- Playwright のテストを実行する場合は必ず `npm run e2e:ci` を使用し、CLI の応答遅延を避ける。
- Lint/フォーマットのルールに従い、既存のコーディングスタイルを維持する。
- エージェントのモデルやプロンプトを調整した際は、変更内容を `docs/agents/CHANGELOG.md` に追記し、いつ・なぜ変更したか残す。

## セキュリティ

- 外部サービスとの通信は HTTPS を利用し、不要な権限を付与しない。必要に応じて IP 制限やスコープの絞り込みを検討する。
- 依存パッケージは定期的に `npm audit` や Renovate 等で確認し、重大な脆弱性を検知したら早めに対応方針を決める。
- エージェントが生成する一時ファイルは最小限とし、機密情報を含む場合は作業完了後に削除する。

## 振り返りと改善

- 大きなタスクを終えたタイミングで `docs/plan.md` に成果・課題を追記し、次のアクションを明確化する。
- 不具合や障害が発生した場合は原因を整理し、再発防止策を `docs/plan.md` または関連ドキュメントに記録する。
- 新しいエージェントや自動化フローを導入する際は、小規模な検証（PoC）を行い、開発効率・コスト・リスクを確認してから本運用に入る。

---

本ガイドはプロジェクトの進行状況に応じて随時見直し、必要に応じて内容を更新すること。
